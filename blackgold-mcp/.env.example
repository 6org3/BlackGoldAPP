# Copia este archivo a .env (gitignored) y completa los valores reales.
# Mismos valores que Dashboard_Premium/.env.local (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY),
# solo sin el prefijo VITE_. Nunca commitear .env ni pegar estas claves en un chat.

SUPABASE_URL=
SUPABASE_ANON_KEY=
